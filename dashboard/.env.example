# CapOwn Dashboard static-export build configuration

# Optional npm registry mirror used by Docker builds.
NPM_REGISTRY=https://registry.npmjs.org

# Host port for the static Dashboard container.
CAPOWN_DASHBOARD_PORT=3000
